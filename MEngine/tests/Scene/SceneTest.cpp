#include <gtest/gtest.h>
#include "Scene/Scene.h"

TEST(SceneTest, EntityManagement)
{
    // Scene scene;
    // auto entity = std::make_shared<Line>(...);
    // scene.addEntity(entity);
    // EXPECT_EQ(scene.entityCount(), 1);
}

TEST(SceneTest, LayerOperations)
{
    // Scene scene;
    // auto layer = scene.createLayer("NewLayer");
    // EXPECT_TRUE(scene.hasLayer("NewLayer"));
}