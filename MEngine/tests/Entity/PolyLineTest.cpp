#include <gtest/gtest.h>
#include "Entity/PolyLine.h"

TEST(PolyLineTest, VertexManagement)
{
    // PolyLine pline;
    // pline.addVertex({0,0});
    // pline.addVertex({5,0});
    // EXPECT_EQ(pline.vertexCount(), 2);
}

TEST(PolyLineTest, BoundingBoxCalculation)
{
    // PolyLine pline({ {0,0}, {5,5}, {2,8} });
    // auto bbox = pline.calculateBoundingBox();
    // EXPECT_DOUBLE_EQ(bbox.width(), 5.0);
}