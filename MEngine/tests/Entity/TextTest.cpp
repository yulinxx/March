#include <gtest/gtest.h>
#include "Entity/Text.h"

TEST(TextTest, ContentInitialization)
{
    // auto text = std::make_shared<Text>();
    // text->setContent("测试文本");
    // EXPECT_EQ(text->content(), "测试文本");
}

TEST(TextTest, FontProperties)
{
    // auto text = std::make_shared<Text>();
    // text->setFont("宋体");
    // text->setSize(12);
    // EXPECT_TRUE(text->font().contains("宋体"));
    // EXPECT_EQ(text->fontSize(), 12);
}