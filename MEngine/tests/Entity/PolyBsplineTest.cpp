#include <gtest/gtest.h>
#include "Entity/CubicBSpline.h"

TEST(PolyBsplineTest, ControlPointCount)
{
    // CubicBSpline spline({...});
    // EXPECT_GE(spline.controlPoints().size(), 4);
}

TEST(PolyBsplineTest, CurveEvaluation)
{
    // CubicBSpline spline(...);
    // auto pt = spline.evaluate(0.5);
    // EXPECT_FALSE(pt.isNull());
}