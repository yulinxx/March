#include <gtest/gtest.h>
#include "DrawData/PointData.h"

TEST(PointDataTest, CoordinateStorage)
{
    // PointData pt(3.14, 2.718);
    // EXPECT_DOUBLE_EQ(pt.x(), 3.14);
    // EXPECT_DOUBLE_EQ(pt.y(), 2.718);
}

TEST(PointDataTest, MoveOperation)
{
    // PointData pt(0,0);
    // pt.moveTo(10, 20);
    // EXPECT_EQ(pt.x(), 10);
}