#include <gtest/gtest.h>
#include "DrawData/CircleData.h"

TEST(CircleDataTest, CenterValidation)
{
    // CircleData circle(...);
    // EXPECT_DOUBLE_EQ(circle.center().x(), 0.0);
    // EXPECT_DOUBLE_EQ(circle.radius(), 5.0);
}

TEST(CircleDataTest, DiameterCalculation)
{
    // CircleData circle(Point(0,0), 5);
    // EXPECT_DOUBLE_EQ(circle.diameter(), 10.0);
}