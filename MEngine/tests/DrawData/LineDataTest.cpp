#include <gtest/gtest.h>
#include "DrawData/LineData.h"

TEST(LineDataTest, EndPointOrder)
{
    // LineData line(Point(0,0), Point(5,5));
    // EXPECT_EQ(line.startPoint().x(), 0);
    // EXPECT_EQ(line.endPoint().y(), 5);
}

TEST(LineDataTest, LengthCalculation)
{
    // LineData line(...);
    // EXPECT_NEAR(line.length(), 7.07, 0.01);
}