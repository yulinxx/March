#include <gtest/gtest.h>
#include "DrawData/DrawData.h"

TEST(DrawDataTest, BaseClassInterface)
{
    // DrawData data;
    // EXPECT_TRUE(data.isValid()); // 验证虚函数实现
}

TEST(DrawDataTest, CloneOperation)
{
    // auto data = std::make_shared<CircleData>(...);
    // auto cloned = data->clone();
    // EXPECT_NE(cloned, nullptr);
}