#include <gtest/gtest.h>
#include "DrawData/ArcData.h"

TEST(ArcDataTest, Initialization)
{
    // ArcData arc(...);
    // EXPECT_DOUBLE_EQ(arc.radius(), 5.0);
    // 验证其他属性
}

TEST(ArcDataTest, BoundingBox)
{
    // 测试包围盒计算
    // ArcData arc(...);
    // auto bbox = arc.boundingBox();
    // EXPECT_FALSE(bbox.isEmpty());
}