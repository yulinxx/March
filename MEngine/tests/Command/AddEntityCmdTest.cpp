#include <gtest/gtest.h>
#include "Command/AddEntityCmd.h"

TEST(AddEntityCmdTest, ExecuteTest)
{
    // 测试命令执行逻辑
    // AddEntityCmd cmd(...);
    // EXPECT_TRUE(cmd.execute());
}

TEST(AddEntityCmdTest, UndoTest)
{
    // 测试撤销逻辑
    // AddEntityCmd cmd(...);
    // cmd.execute();
    // EXPECT_TRUE(cmd.undo());
}