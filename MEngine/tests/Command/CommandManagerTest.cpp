#include <gtest/gtest.h>
#include "Command/CommandManager.h"

TEST(CommandManagerTest, CommandSubmission)
{
    // 测试命令提交
    // CommandManager manager;
    // auto cmd = std::make_shared<AddEntityCmd>(...);
    // EXPECT_TRUE(manager.submit(cmd));
}

TEST(CommandManagerTest, UndoRedoFlow)
{
    // 测试撤销/重做流程
    // CommandManager manager;
    // 执行若干命令后验证状态
}